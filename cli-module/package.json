{
  "name": "<%= name %>",
  "description": "<%= description %>",
  "version": "0.0.0",
  "keywords": [
    ""
  ],
  "repository": "<%= githubUsername %>/<%= name %>",
  "dependencies": {
    "meow": "^3.3.0"
  },
  "devDependencies": {
    "babel": "^5.5.6",
    "mocha": "^2.2.5",
    "rimraf": "^2.4.3"
  },
  "main": "index.es5.js",
  "bin": "cli.es5.js",
  "scripts": {
    "test": "mocha --require babel/register",
    "transpile": "babel --stage 0 index.js --out-file index.es5.js && babel --stage 0 cli.js --out-file cli.es5.js",
    "prepublish": "npm run transpile",
    "postpublish": "rimraf index.es5.js cli.es5.js"
  },
  "author": "<%= authorName %> <<%= authorEmail %>> (<%= authorWebsite %>)",
  "license": "MIT"
}
